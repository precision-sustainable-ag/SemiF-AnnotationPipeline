general:
  db: test
  multitask: false
  task: segment_vegetation
  name: test_exp_v2
  workdir: ${hydra:runtime.cwd}
  datadir: ${general.workdir}/data
  imagedir: ${general.datadir}/autosfm-trial/semif-trial/semif-trial
  autosfm_results: ${general.datadir}/autosfm_results
  model_path: ${general.workdir}/models/Models/plant_detector/exp/weights/best.pt
  reference_path: ${general.datadir}/autosfm-trial/semif-trial/reference
  raw_label_dir: ${general.datadir}/autosfm-trial/semif-trial/annotations
  label_save_path: ${general.datadir}/autosfm-trial/results
  autosfm_metadata_savedir: null
  save_detection: true
  detectioncsv_dir: ${general.imagedir}
  vi: exg
  class_algorithm: kmeans
  num_classes: 1
  clear_border: true
  save_to_database: false
