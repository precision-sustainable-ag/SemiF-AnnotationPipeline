batch_id: ??? # Control the batch_id through the command line
workdir: ${hydra:runtime.cwd}  # where the code is executed

blob_cutout: semifield-cutouts
blob_model: semifield-models
blob_synth: semifield-synth

datadir: ${workdir}/data
synthdir: ${datadir}/${blob_synth}
cutoutdir: ${datadir}/${blob_cutout}
modeldir: ${datadir}/${blob_model}

cutout_batchdir: ${cutoutdir}/${batch_id}
potdir: ${synthdir}/pots
backgrounddir: ${synthdir}/backgrounds
